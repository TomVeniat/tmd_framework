modules:
    model:
        _name: mnist_classifier

datasets:
    train:
        _name: mnist
        train: True
        batch_size: &batch_size 22

    val:
        _name: mnist
        train: False
        batch_size: *batch_size

optimizers:
    optimizer:
        _name: sgd
        _modules: model
        lr: 0.001
        # scheduler:
        #     _name: exponential
        #     gamma: 0.99

experiment:
    _name: mnist
    nepochs: 10
    use_tqdm: True